# Ignore node modules and lockfiles
node_modules

# Logs
npm-debug.log*
*.log

# Build output and runtime caches
coverage
monzo_cache
budget
data

# Environment files
.env

# Git and Docker metadata
.git
.gitignore
.dockerignore

# Docker and compose files (not needed inside container)
# Docker and compose files (not needed inside container)
Dockerfile
docker-compose.yml

# Ignore deployment helper script
deploy.sh

# Tests and related files
tests

# IDE and OS files
.vscode
.idea
*.iml
.DS_Store