# Sample YAML configuration file.
# Copy to config.yaml or config.yml in the project root and adjust settings as needed.

# Sync schedule (once an hour on the hour)
SYNC_CRON: '45 * * * *'
SYNC_CRON_TIMEZONE: UTC

# Disable cron scheduling (daemon mode only; default: false)
DISABLE_CRON_SCHEDULING: false

# Port for web UI server (daemon mode only)
HTTP_PORT: 3000

# Base directory for data storage (mapping file)
DATA_DIR: './data'

# Directory for downloaded Actual Budget files
BUDGET_DIR: './data/budget'

# Monzo OAuth endpoints (override only if needed)
MONZO_AUTH_ENDPOINT: 'https://auth.monzo.com'
MONZO_API_ENDPOINT: 'https://api.monzo.com'
MONZO_TOKEN_PATH: '/oauth2/token'
